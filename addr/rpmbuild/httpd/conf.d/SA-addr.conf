<IfModule mod_alias.c>
    #
    # Settings for the addr web application:
    #
    ScriptAlias /addr/ "/u01/app/addr/www/cgi/"
    <Directory "/u01/app/addr/www/cgi">
        AllowOverride All
        Options None
        Order allow,deny
        Allow from all
    </Directory>
</IfModule>
