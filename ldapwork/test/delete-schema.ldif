ou=Rpc,dc=tomax,dc=com

ou=Networks,dc=tomax,dc=com

nisMapName=netgroup.byhost,dc=tomax,dc=com

nisMapName=netgroup.byuser,dc=tomax,dc=com

ou=Netgroup,dc=tomax,dc=com

ou=Hosts,dc=tomax,dc=com

ou=Services,dc=tomax,dc=com

ou=Mounts,dc=tomax,dc=com

ou=Aliases,dc=tomax,dc=com

ou=Protocols,dc=tomax,dc=com
